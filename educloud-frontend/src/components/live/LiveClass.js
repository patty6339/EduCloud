import React from 'react';

const LiveClass = ({ id }) => {
  return (
    <div>
      <h2>Live Class: {id}</h2>
      <p>Live class feature under development...</p>
    </div>
  );
};

export default LiveClass;
